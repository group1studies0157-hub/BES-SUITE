# Bridge Engineering Suite v2.0 — Dependencies
# Install: pip install -r requirements.txt

# ── GUI ──────────────────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Computer Vision (geometry detection) — local, free ───────────────────────
opencv-python-headless>=4.8.0
numpy>=1.24.0

# ── CAD file output — open source ────────────────────────────────────────────
ezdxf>=1.4.0

# ── Image handling ────────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── PDF support ───────────────────────────────────────────────────────────────
PyMuPDF>=1.28.0
pdfplumber>=0.11.0       # Bore-log PDF table extraction

# ── AI Vision — Claude (primary OCR + shape recognition) ─────────────────────
anthropic>=0.25.0
# Set ANTHROPIC_API_KEY env var, or enter key directly in the app Settings panel

# ── AI Vision — Google Gemini (dual-key auto-fallback, tried first) ───────────
google-generativeai>=0.8.0
# Set GEMINI_API_KEY env var, or enter key directly in the app Settings panel
# Without this package the app skips Gemini and uses Claude automatically.

# ── Local OCR fallback (free, offline, optional) ──────────────────────────────
pytesseract>=0.3.10
# Requires Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki (Windows)

# ── Bore-log Excel parsing ───────────────────────────────────────────────────
xlrd>=2.0.1              # .xls reading (legacy Excel format)
openpyxl>=3.1.0          # .xlsx reading

# ── DXF repair pipeline (GAD Generator → DXF Repair tab) ─────────────────────
matplotlib>=3.8.0        # optional: higher-fidelity render for validation diffs
scikit-learn>=1.3.0      # optional: DBSCAN clustering fallback (greedy used otherwise)


# ── Google Sheets sync — service account, no browser login (core/sheets_sync.py)
gspread>=6.0.0
google-auth>=2.30.0
# Reads the service-account key from secrets/bes_service_account.json
# (or the BES_SHEETS_CREDENTIALS env var). See core/sheets_sync.py for setup.
